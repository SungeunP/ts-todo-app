import { ListItem, withStyles } from '@material-ui/core';

const style = {
  root: {
    height: '70px',
    lineHeight: '70px',
    padding: '0px 8px',
  },
}

export default withStyles(style)(ListItem)